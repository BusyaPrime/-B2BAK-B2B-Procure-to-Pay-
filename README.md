<img width="1920" height="1080" alt="Снимок экрана (875)" src="https://github.com/user-attachments/assets/fa5c22ce-2fc8-4675-a59a-ee691ed8e667" />
# 🚀 B2BAK — B2B платформа закупочного цикла между компаниями

|  |
|---|
| **B2BAK — это B2B-платформа, которая цифровизирует путь от потребности покупателя до оплаты поставщику. Проект собран как связанная система с ролями, изоляцией данных по организациям, жизненным циклом заявки и сделки, аудитом действий, уведомлениями, инвайтами, коммуникациями и профилем пользователя.** |

---

## 📌 Коротко о продукте

Платформа закрывает полный закупочный цикл:

- Buyer создаёт запрос на услугу/работу
- Vendor отправляет коммерческое предложение
- Buyer делает shortlist и выбирает победителя
- Формируется deal
- Создаётся invoice
- Сделка закрывается статусом paid

Вокруг этого реализованы роли, изоляция данных, история действий, уведомления и командная работа.

---

## 🧰 Технологии

### Backend

- Python 3.12
- FastAPI (REST API)
- SQLAlchemy 2 (ORM)
- Alembic (миграции)
- Pydantic v2 + pydantic-settings (схемы и конфиг)
- python-jose (JWT)
- passlib + bcrypt (пароли)
- Redis (state/rate limit/queue infra)
- RQ (фоновый воркер)
- httpx (OAuth-запросы)

### Frontend

- Next.js 14 (Pages Router)
- React 18
- TypeScript (strict)
- Tailwind CSS + кастомные CSS-переменные
- Sonner (toasts)
- Playwright (e2e smoke)

### Инфраструктура

- Docker Compose
- Сервисы: frontend, backend, postgres, redis, worker

---

## 🏗 Архитектура

Проект разделён по слоям:

```text
frontend/ — UI, страницы, клиентская логика, вызовы API
backend/ — API, доменные правила, модели БД, auth, middleware
infra/ — локальное окружение для запуска всего стека
docs/ — дефолты и справочная конфигурация
```

Ключевой принцип: изоляция данных идёт по org_id.  
Это мульти-tenant подход: доступ ограничен ролью и принадлежностью к организации.

---

## 👥 Роли и доступ

Роли:

- ORG_OWNER
- ADMIN
- BUYER
- VENDOR
- VIEWER

Как используется:

- Buyer: создаёт и управляет заявками, выбирает победителя, ведёт сделку к оплате
- Vendor: видит открытые заявки, отправляет и обновляет предложения
- Admin/Owner: управление внутри организации и контроль
- Viewer: доступ на просмотр

На backend доступ проверяется через dependency require_roles(...).

---

## 🔐 Аутентификация и регистрация

Есть три режима входа:

- Email + password
- OAuth Google
- OAuth GitHub

### Классическая регистрация

Пользователь указывает:

- email
- пароль
- название workspace/company
- роль (BUYER / VENDOR / VIEWER)

На backend:

- создаётся Organization
- создаётся User
- если роль buyer/vendor — создаётся профиль BuyerProfile / VendorProfile
- выставляются auth cookies (access, refresh)

### OAuth-поток

- /auth/oauth/{provider}/start генерирует state и кладёт его в Redis с TTL
- callback /oauth/[provider] получает code + state
- backend делает обмен code -> token -> profile через httpx
- если email уже существует — выполняется логин
- если пользователь новый — выдаётся verify-token и предлагается выбрать роль
- после выбора роли регистрация завершается

---

## 🍪 Cookie/JWT безопасность

Auth работает через HttpOnly cookies:

- b2bak_access
- b2bak_refresh

Параметры:

- SameSite=Lax
- secure управляется env
- refresh endpoint переиздаёт cookies

Дополнительно:

- rate limiting на login/register/refresh через Redis (incr/expire)
- единый формат ошибок application/problem+json
- request id middleware и проброс X-Request-ID

---

## 🗃 Доменные сущности (БД)

Основные таблицы:

- organizations
- users
- buyer_profiles, vendor_profiles
- requests
- quotes
- deals
- invoices
- messages
- notifications
- invites
- audit_log
- idempotency_keys

---

## 🔄 Статусы (workflow)

Request:

- DRAFT -> PUBLISHED/QUOTING -> SHORTLIST -> AWARDED -> CLOSED

Quote:

- SUBMITTED -> UPDATED -> WITHDRAWN/ACCEPTED/REJECTED

Deal:

- NEGOTIATION -> CONTRACT -> INVOICED -> PAID -> ARCHIVED

Invoice:

- DRAFT -> SENT -> PAID -> VOID

Переходы ограничены роутами, чтобы нельзя было “перескочить” в нелогичное состояние.

---

## 🌐 Backend API (что реализовано)

Auth:

- POST /auth/login
- POST /auth/register
- POST /auth/logout
- POST /auth/refresh
- GET /auth/me
- GET /auth/profile
- PATCH /auth/profile
- OAuth start/exchange/complete-registration

Requests / Quotes / Deals:

- CRUD-операции для requests/quotes с доменными ограничениями
- publish/shortlist/award для request
- create-invoice/mark-paid для deal

Коммуникации и контроль:

- deal messages
- audit listing
- notifications list/read/emit-job/stream (SSE)
- invites list/create/accept
- assistant suggest (helper endpoint)

---

## 🧭 Frontend: маршруты

Основные страницы:

- login/signin/classic-login — единая auth-страница с табами Log In / Sign Up
- OAuth callback страница с выбором роли
- profile — настройки пользователя
- dashboard — обзор
- кабинеты по ролям: buyer / vendor / admin / viewer
- marketplace: список requests, карточка request, список deals
- отдельная форма “precise request” для buyer

---

## 👤 Профиль пользователя

На profile:

- display name
- avatar upload (data URL)
- почта регистрации
- роль
- организация
- дата регистрации
- переключение темы (dark/light)
- переключение языка (en/ru)
- сохранение в backend (PATCH /auth/profile)
- logout

Тема применяется глобально через _app и custom event.

---

## 🔔 Уведомления и realtime

Два формата:

- REST: list/read
- SSE: /notifications/stream

Для dev-демо есть endpoint emit-job, чтобы вручную создавать notification-события и проверять UI.

---

## 🌱 Seed и демо-аккаунты

Seeder создаёт:

- организацию Demo Inc
- пользователей buyer/vendor/admin/owner/viewer
- тестовые request/quote/deal/invoice/messages/notifications/invites/audit

После поднятия проект сразу готов к демонстрации.
